[package]
name = "project"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.2", features = ["derive"] }
libc = { version = "0.2.153", features = ["extra_traits"] }
procfs = "0.16.0"
